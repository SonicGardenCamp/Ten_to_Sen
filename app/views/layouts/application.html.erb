<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Camp Ten To Sen" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= action_cable_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body>
    <div class="titlecontainer">
      <div class="titlemenubox">
        <% if controller_name == "rooms" %>
          <div class="header-menu-container"
               <% if action_name == "index" %>
                 data-controller="bgm"
                 data-bgm-files-value="<%= [asset_path('bgm1.mp3'), asset_path('bgm2.mp3'), asset_path('bgm3.mp3')].to_json %>"
               <% end %>>

            <div style="display: flex; gap: 10px; align-items: center;">
              <% if action_name == "index" %>
                <button data-action="click->bgm#toggle" data-bgm-target="toggleButton" class="logout-btn">🔊 BGM</button>
                <button data-action="click->bgm#next" class="logout-btn">⏭️</button>
              <% end %>
            </div>

            <div class="greeting">
               <% if current_user %>
                こんにちは、<%= current_user.username %> さん
                <%= button_to "ログアウト", destroy_user_session_path, method: :delete, class: "logout-btn" %>
              <% else %>
                ゲストでアクセス中です
               <% end %>
            </div>

            <% if action_name == "index" %>
              <audio data-bgm-target="player" preload="auto" style="display: none;"></audio>
            <% end %>
          </div>
        <% end %>
      </div>
    </div>
    <div class="titletext">
        <h3 class="titletext_sub2"> 高速しりとりバトル </h3>
        <h1 class="titletext_sub"> WORD CHASER </h1>
    </div>

    <%= yield %>
  </body>
</html>

<% if controller_name == "rooms" && action_name == "index" %>
<style>
/* レスポンシブ対応 */
@media (max-width: 768px) {
  /* モバイルでは縦並びに */
  .header-menu-container {
    flex-direction: column;
    gap: 10px;
    align-items: flex-start;
  }

  .titlecontainer .titlemenubox .greeting {
    align-self: flex-end;
  }
}
</style>
<% end %>